# #fp — Functional
Pure functions, immutable data, composition. Mutation is the exception.

∀ code: immutable data, pure functions, composition, values over objects. ∀ mutation: pure alternative stated, forcing reason named. fp ∩ {SharedMutableState} = ∅    -- HARD CONSTRAINT
Expressions over statements. Transformations (map/filter/reduce) over imperative loops. Higher-order functions over redundant logic.
Local mutation tolerated when substantially cleaner — justify and contain.
