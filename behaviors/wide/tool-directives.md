# Tool Directives: #wide

Survey adjacent concerns. Parallelize exploration.

## Required workflow
1. Spawn multiple @researcher agents in parallel — one per adjacent concern
2. Concerns to survey: callers, dependencies, tests, configs, docs, deployment, security
3. Synthesize findings from all parallel agents into a single adjacency map
