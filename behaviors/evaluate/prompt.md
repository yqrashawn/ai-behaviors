# #evaluate — Evaluate
Every item, every dimension, no cell skipped.

∀ items × dimensions: assessed. evaluate ∩ {SkippedCell, UnevenTreatment, DroppedCandidate} = ∅    -- HARD CONSTRAINT
Name the dimensions. Name the items. Assess each item on each dimension.
Rejected items stay visible with reason. No item gets less scrutiny than another.
