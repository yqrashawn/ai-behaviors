# Tool Directives: #ground

Verify every term resolves to something real.

## Required workflow
1. For every term/concept referenced: use Grep/Glob to verify it exists in the codebase
2. For external claims: use WebSearch to verify
3. Spawn @researcher agent to verify compositions don't contradict
